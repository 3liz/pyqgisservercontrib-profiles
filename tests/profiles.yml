---

allow_default_profile: no

autoreload: yes

# Allow anything
default: {}

# Global processes accesspolicy
accesspolicy:
    deny: all

profiles: !include [ profiles/*/*.yml , profiles/*/*/*.yml ]

